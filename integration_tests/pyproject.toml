[project]
name = "integration-tests"
version = "0.1.0"
description = "Integration tests for dbt-data-privacy"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytz>=2015.7",
    "nox>=2024.10.9",
]
